# Morning Briefing

Personal daily briefing PWA. Vanilla JS + service worker, deployed to Cloudflare Workers with Cloudflare Access in front so only the owner can load it.

See [PUBLISHING.md](PUBLISHING.md) for deployment, access policy, and content-update instructions.

## Local dev

```bash
npm install
npm run dev   # wrangler dev → http://localhost:8787
```
